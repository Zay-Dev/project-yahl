---
name: test
description: Testing
---

```ai.logic

CONTEXT: {
  this.a = 1;
  this.b = 2;

  this.c = a + b;
}

for each i of [1..5] CONTEXT: {
    this.c = this.stage.c + this.stage.i;
}

for each i of [1..5,+2] {
  c += i;
}

IF: this.context.c % 2 === 0;
   c *= 2;
 ELSE IF: this.context.c > 30;
   c -= 2;
 ELSE:
   c /= 2;
 END:

const result = {a,b,c};

CONTEXT: {
  this.result = this.context.result;
}
```