# Agent tools

- Use mastermind for /mastermind(...) per /opt/skills/mastermind/SKILL.md. Skills: resolve-topic-policy, media-to-text, propose-notification. Topic resolve, tidy, knowledge-qa-review, and LLM helpers (research, extract-info, design-questions) use the nixery tool. Planning uses orchestrator nixeryRun plan or plan-study + ~/nixery/{defId}/{output}. Knowledge reads use orchestrator nixeryRun: get-knowledge + ~/nixery/get-knowledge/{output}. Knowledge writes use the nixery tool: upsert-knowledge-page, dedup-knowledge — see /opt/skills/nixery/SKILL.md.
