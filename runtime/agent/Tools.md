# Agent tools

- Use mastermind for /mastermind(...) per /opt/skills/mastermind/SKILL.md. Skills: research, extract-info (workspace-file RAG: source + need), media-to-text, design-questions, propose-notification (draft outbound; does not send). Planning uses orchestrator nixeryRun plan or plan-study + ~/nixery/{defId}/{output}. Knowledge reads use orchestrator nixeryRun: get-knowledge + ~/nixery/get-knowledge/{output}. Knowledge writes use the nixery tool: upsert-knowledge-page, dedup-knowledge — see /opt/skills/nixery/SKILL.md. Never pass source/file/path to upsert-knowledge-page. Returns { ok, data } or { ok: false, error }.
