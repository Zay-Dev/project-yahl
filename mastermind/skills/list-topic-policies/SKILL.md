---
name: list-topic-policies
description: List knowledge topic refresh policies and folder metadata (no corpus content)
---

# list-topic-policies

`/mastermind(list-topic-policies)` returns `{ items: TTopicPolicyRow[] }` — slug, refresh policy, `updatedAt`, counts only.
