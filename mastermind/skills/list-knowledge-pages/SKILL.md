---
name: list-knowledge-pages
description: List wiki pages under a topic namespace.
---

# list-knowledge-pages

```json
{
  "skill": "list-knowledge-pages",
  "args": { "topic": "hk-weather" }
}
```

Returns `{ topic, pages: [{ page, pagePath, source }] }` from GraphQL or export mirror.
