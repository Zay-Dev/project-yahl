---
name: evaluate-knowledge-refresh
description: Deterministic staleness scan for scheduled knowledge refresh
---

# evaluate-knowledge-refresh

`/mastermind(evaluate-knowledge-refresh)` returns `{ checkedAt, staleTopics: [{ canonical, interval, scopes, reason }] }`.
